# Frontend environment variables (Vite prefixes with VITE_)
# Not needed in development — Vite proxy handles /api requests
# Set this for production builds pointing to your API server:
VITE_API_URL=http://localhost:5000/api
